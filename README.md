# Finance Clarity App (WIP)

## Repo Structure
- apps/mobile — React Native (Expo) mobile app (primary)
- apps/web — Web (later)
- services/api — Backend API (Node/TypeScript)
- services/worker — Scheduled jobs (later)
- packages/shared — Shared types/enums
- packages/ui — Shared UI primitives
- infra — Deployment/config (later)

## Development (will be filled as we scaffold)
This repo is being built step-by-step. The following scripts will be added:
- install dependencies
- run mobile app
- run backend API
- run tests/lint/typecheck
